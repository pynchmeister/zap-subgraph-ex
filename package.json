{
  "name": "zapRegistry",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ pynchmeister/zapRegistry",
    "create-local": "graph create --node http://localhost:8020/ pynchmeister/zapRegistry",
    "remove-local": "graph remove --node http://localhost:8020/ pynchmeister/zapRegistry",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 pynchmeister/zapRegistry"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.20.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
